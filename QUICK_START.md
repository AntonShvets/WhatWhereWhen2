# Быстрый старт

## 🚀 Серверы запущены!

### Доступные адреса:

- **Админка (Frontend):** http://localhost:3001
- **ТВ-Клиент:** http://localhost:3003
- **Backend API:** http://localhost:3002
- **Socket.io:** ws://localhost:3002/game

### Что можно сделать сейчас:

1. **Откройте админку** в браузере: http://localhost:3001

2. **Проверьте звуки:**
   - На главном экране есть блок "Звуковые Эффекты"
   - Доступны звуки: Гонг, Интро, Пауза, Волчок, Музыка Победителей
   - Звуки загружены в `/backend/uploads/`

3. **Управление данными:**
   - **Знатоки:** http://localhost:3001/experts
   - **Телезрители и Вопросы:** http://localhost:3001/viewers-questions

### Важно:

⚠️ **База данных:** Убедитесь, что:
- PostgreSQL запущен
- База данных `whatwherewhen2` создана
- Схема применена: `psql -U postgres -d whatwherewhen2 -f database/schema.sql`

⚠️ **Если backend не отвечает:**
- Проверьте подключение к БД
- Посмотрите логи в терминале, где запущен backend
- Убедитесь, что переменные окружения настроены

### Следующие шаги:

1. Создайте активную игру через API или напрямую в БД
2. Добавьте знатоков через интерфейс `/experts`
3. Добавьте телезрителей и вопросы через `/viewers-questions`
4. Начните игру на главном экране

### Остановка серверов:

Нажмите `Ctrl+C` в терминалах, где запущены серверы, или:

```bash
# Остановить все Node.js процессы
pkill -f "nest start"
pkill -f "next dev"
```

### Полезные команды:

```bash
# Проверить статус портов
lsof -ti:3001 -ti:3002

# Проверить подключение к БД
psql -U postgres -d whatwherewhen2 -c "SELECT COUNT(*) FROM games;"

# Посмотреть логи backend (если запущен в отдельном терминале)
# Логи отображаются автоматически при запуске через npm run start:dev
```

