# Инструкция по запуску WhatWhereWhen2

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ 
- PostgreSQL 14+
- npm или yarn

---

## 📦 Установка и запуск

### 1. База данных

```bash
# Создать базу данных
createdb whatwherewhen2

# Применить схему
psql -d whatwherewhen2 -f database/schema.sql
```

### 2. Backend (NestJS)

```bash
cd backend

# Установить зависимости
npm install

# Настроить переменные окружения (опционально)
# Создать .env файл:
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=your_username
# DB_PASSWORD=your_password
# DB_NAME=whatwherewhen2
# PORT=3002

# Запустить в режиме разработки
DB_USERNAME=$(whoami) DB_PASSWORD="" npm run start:dev
```

Backend будет доступен на: **http://localhost:3002**

### 3. Админка (Next.js)

```bash
cd frontend/admin

# Установить зависимости
npm install

# Настроить переменные окружения (опционально)
# Создать .env.local:
# NEXT_PUBLIC_API_URL=http://localhost:3002
# NEXT_PUBLIC_SOCKET_URL=http://localhost:3002

# Запустить
npm run dev
```

Админка будет доступна на: **http://localhost:3001**

### 4. ТВ-Клиент (Next.js)

```bash
cd frontend/tv-client

# Установить зависимости
npm install

# Настроить переменные окружения (опционально)
# Создать .env.local:
# NEXT_PUBLIC_API_URL=http://localhost:3002
# NEXT_PUBLIC_SOCKET_URL=http://localhost:3002

# Запустить
npm run dev
```

ТВ-Клиент будет доступен на: **http://localhost:3003**

---

## 🔊 Звуковые файлы

### Размещение звуковых файлов

Звуковые файлы должны быть размещены в папке:

```
backend/uploads/
```

### Поддерживаемые форматы

- **Аудио:** MP3, WAV, OGG
- **Рекомендуется:** MP3 для максимальной совместимости

### Необходимые звуки

Для полной функциональности загрузите следующие файлы:

- `gong-1.mp3` - Гонг
- `intro.mp3` - Интро
- `pause1.mp3` - Пауза
- `volchok.mp3` - Волчок
- `winners_music.mp3` - Музыка победителей

### Проверка доступа к звукам

После размещения файлов проверьте доступность:

```bash
# Проверить наличие файлов
ls -la backend/uploads/

# Проверить доступность через HTTP
curl http://localhost:3002/uploads/gong-1.mp3
```

---

## 🔧 Переменные окружения

### Backend

Создайте файл `backend/.env`:

```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_username
DB_PASSWORD=your_password
DB_NAME=whatwherewhen2

# Server
PORT=3002
NODE_ENV=development
```

### Frontend (Админка)

Создайте файл `frontend/admin/.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3002
NEXT_PUBLIC_SOCKET_URL=http://localhost:3002
```

### Frontend (ТВ-Клиент)

Создайте файл `frontend/tv-client/.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3002
NEXT_PUBLIC_SOCKET_URL=http://localhost:3002
```

---

## 🎮 Первый запуск

### 1. Создать активную игру

```bash
curl -X POST http://localhost:3002/api/games \
  -H "Content-Type: application/json" \
  -d '{"status":"active","experts_score":0,"viewers_score":0}'
```

Или через админку: откройте http://localhost:3001 и создайте игру через интерфейс.

### 2. Открыть приложения

- **Админка:** http://localhost:3001
- **ТВ-Клиент:** http://localhost:3003

### 3. Проверить подключение

- В админке должен отображаться счет игры
- В ТВ-клиенте должен быть индикатор подключения (зеленый в правом верхнем углу)
- Счет должен отображаться на обоих экранах

---

## 🐛 Troubleshooting

### Backend не запускается

**Проблема:** Ошибка подключения к БД

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Проверьте учетные данные в переменных окружения
3. Убедитесь, что база данных создана

```bash
# Проверить подключение к PostgreSQL
psql -U your_username -d whatwherewhen2 -c "SELECT 1;"
```

### Админка показывает "Network Error"

**Проблема:** Не может подключиться к backend

**Решение:**
1. Убедитесь, что backend запущен на порту 3002
2. Проверьте переменную `NEXT_PUBLIC_API_URL`
3. Проверьте CORS настройки в backend

```bash
# Проверить доступность backend
curl http://localhost:3002/api/games
```

### ТВ-Клиент не подключается к Socket.io

**Проблема:** Индикатор подключения красный

**Решение:**
1. Проверьте, что backend запущен
2. Проверьте переменную `NEXT_PUBLIC_SOCKET_URL`
3. Откройте консоль браузера (F12) для просмотра ошибок

### Звуки не воспроизводятся

**Проблема:** Кнопка нажата, но звука нет

**Решение:**
1. Проверьте наличие файлов в `backend/uploads/`
2. Проверьте доступность через HTTP:
   ```bash
   curl http://localhost:3002/uploads/gong-1.mp3
   ```
3. Проверьте консоль браузера на ошибки
4. Убедитесь, что браузер не блокирует автовоспроизведение

---

## 📊 Проверка производительности

### Измерение задержек

1. **Обновление счета:**
   - Нажмите +1 в админке
   - Засеките время до обновления в ТВ-клиенте
   - Целевое время: < 100ms

2. **Смена контента:**
   - Нажмите "Показать Вопрос" в админке
   - Засеките время до отображения в ТВ-клиенте
   - Целевое время: < 200ms

3. **Воспроизведение звука:**
   - Нажмите кнопку звука в админке
   - Засеките время до начала воспроизведения
   - Целевое время: < 100ms

---

## 🚀 Production Deployment

### Backend

```bash
cd backend
npm run build
NODE_ENV=production npm run start:prod
```

### Frontend (Админка)

```bash
cd frontend/admin
npm run build
npm start
```

### Frontend (ТВ-Клиент)

```bash
cd frontend/tv-client
npm run build
npm start
```

### Рекомендации для production

1. Используйте reverse proxy (nginx) для статики
2. Настройте HTTPS для безопасности
3. Используйте переменные окружения для конфигурации
4. Настройте мониторинг и логирование
5. Используйте process manager (PM2) для Node.js процессов

---

## 📝 Структура проекта

```
WhatWhereWhen2/
├── backend/              # NestJS API + Socket.io
│   ├── src/
│   ├── uploads/          # Звуковые файлы
│   └── package.json
├── frontend/
│   ├── admin/            # Админка (Next.js)
│   └── tv-client/        # ТВ-Клиент (Next.js)
├── database/
│   └── schema.sql        # SQL схема
└── README.md
```

---

## 🔗 Полезные ссылки

- **Админка:** http://localhost:3001
- **ТВ-Клиент:** http://localhost:3003
- **Backend API:** http://localhost:3002
- **Socket.io:** ws://localhost:3002/game

---

## ✅ Чек-лист перед запуском

- [ ] PostgreSQL установлен и запущен
- [ ] База данных создана и схема применена
- [ ] Звуковые файлы загружены в `backend/uploads/`
- [ ] Backend запущен и отвечает на порту 3002
- [ ] Админка запущена на порту 3001
- [ ] ТВ-Клиент запущен на порту 3003
- [ ] Активная игра создана
- [ ] Все клиенты подключены к Socket.io

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи backend: `tail -f /tmp/backend.log`
2. Проверьте консоль браузера (F12)
3. Проверьте подключение к БД
4. Проверьте переменные окружения
5. См. раздел Troubleshooting выше

