# ТВ-Клиент для WhatWhereWhen2

Полноэкранное приложение для отображения игры на большом экране.

## 🎯 Особенности

- **Полноэкранный режим** - Оптимизирован для больших экранов
- **Real-time обновления** - Мгновенная синхронизация через Socket.io
- **Крупный дизайн** - Большие шрифты и элементы для удобного просмотра
- **Автоматическое управление** - "Тупой" клиент, просто отображает команды от админки

## 🚀 Запуск

### Установка зависимостей

```bash
cd frontend/tv-client
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

ТВ-клиент будет доступен на: **http://localhost:3003**

### Запуск в production

```bash
npm run build
npm start
```

## 📺 Использование

### Доступ к ТВ-клиенту

1. **С активной игрой:**
   ```
   http://localhost:3003?gameId=YOUR_GAME_ID
   ```

2. **Без указания gameId:**
   - Клиент автоматически загрузит активную игру
   - Если активной игры нет, отобразит сообщение об ошибке

### Управление через админку

ТВ-клиент автоматически реагирует на команды из админки:

1. **Обновление счета** - Счет обновляется мгновенно
2. **Команды display_status** - Изменение отображаемого контента
3. **Команды play_sound** - Воспроизведение звуков

## 🎨 Отображаемый контент

### Счет
- Постоянно видимый в верхней части экрана
- Крупные цифры с подсветкой
- Обновляется в реальном времени

### Основная область

В зависимости от команды `display_status` отображается:

1. **Вопрос** (`content: 'question'`)
   - Крупный текст вопроса
   - Встроенный плеер для видео/изображений/аудио
   - Опциональный таймер

2. **Телезритель** (`content: 'viewer'`)
   - Фото телезрителя (круглое, крупное)
   - Имя и город

3. **Ответ** (`content: 'answer'`)
   - Правильный ответ крупным шрифтом
   - Зеленый цвет для выделения

4. **Счет** (`content: 'score'`)
   - Большой счет в центре экрана

5. **Черный экран** (`content: 'black'`)
   - Пустой черный экран

## 🔊 Звуки

ТВ-клиент автоматически воспроизводит звуки при получении команды `play_sound`:

- Звуковые файлы должны быть в `/backend/uploads/`
- Поддерживаются форматы: MP3, WAV
- Громкость настраивается через команду

## 🔌 Socket.io События

### Получаемые события:

- `score:update` - Обновление счета
- `display:change` - Изменение отображаемого контента
- `play_sound` - Команда воспроизвести звук
- `game:update` - Обновление игры

### Отправляемые события:

- `game:join` - Присоединение к игре

## 🎨 Дизайн

- **Фон:** Черный (#000000)
- **Текст:** Белый, крупные шрифты (2rem - 10rem)
- **Акценты:** Золотой (#FFD700)
- **Анимации:** Плавные появления (fade-in, text-appear)

## 📱 Адаптивность

Дизайн оптимизирован для:
- Больших экранов (TV, проекторы)
- Полноэкранного режима
- Горизонтальной ориентации

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3002
NEXT_PUBLIC_SOCKET_URL=http://localhost:3002
```

### Изменение порта

В `package.json` измените порт:

```json
{
  "scripts": {
    "dev": "next dev -p 3003"
  }
}
```

## 🐛 Troubleshooting

### ТВ-клиент не подключается к Socket.io

1. Проверьте, что backend запущен на порту 3002
2. Проверьте CORS настройки в backend
3. Откройте консоль браузера (F12) для просмотра ошибок

### Звуки не воспроизводятся

1. Убедитесь, что звуковые файлы загружены в `/backend/uploads/`
2. Проверьте URL звуков в консоли браузера
3. Убедитесь, что backend раздает статические файлы из `/uploads/`

### Контент не обновляется

1. Проверьте подключение к Socket.io (индикатор в правом верхнем углу)
2. Убедитесь, что админка отправляет команды
3. Проверьте логи в консоли браузера

## 📝 Структура проекта

```
frontend/tv-client/
├── components/
│   ├── ScoreDisplay.tsx      # Отображение счета
│   ├── QuestionDisplay.tsx   # Отображение вопроса
│   ├── ViewerDisplay.tsx     # Отображение телезрителя
│   ├── AnswerDisplay.tsx     # Отображение ответа
│   └── SoundPlayer.tsx       # Воспроизведение звуков
├── lib/
│   ├── socket.ts             # Socket.io клиент
│   └── api.ts                # REST API клиент
├── pages/
│   ├── _app.tsx              # Главный компонент приложения
│   └── index.tsx              # Главная страница ТВ-клиента
└── styles/
    └── globals.css            # Глобальные стили
```

## 🎮 Интеграция с админкой

ТВ-клиент работает в паре с админкой:

1. **Админка** отправляет команды через Socket.io
2. **ТВ-клиент** получает команды и отображает контент
3. **Backend** синхронизирует состояние между админкой и ТВ-клиентом

### Пример работы:

1. Админ нажимает "Показать Вопрос" в админке
2. Админка отправляет `display:update` с `display_status: { content: 'question', ... }`
3. Backend рассылает `display:change` всем клиентам в комнате игры
4. ТВ-клиент получает событие и отображает вопрос

## 🚀 Production Deployment

Для production:

1. Соберите проект: `npm run build`
2. Запустите: `npm start`
3. Настройте reverse proxy (nginx) для статики
4. Используйте HTTPS для безопасности

## 📄 Лицензия

MIT

