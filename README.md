# WhatWhereWhen2

Интерактивная система управления игрой "Что? Где? Когда?" с веб-админкой и ТВ-клиентом.

## 🏗️ Технологический стек

### Backend (API + Realtime)
- **Node.js** - JavaScript runtime
- **NestJS** - Прогрессивный Node.js фреймворк для построения эффективных и масштабируемых серверных приложений
  - Структурированная архитектура (модули, контроллеры, сервисы)
  - Встроенная поддержка TypeScript
  - Dependency Injection
  - Декораторы и валидация
- **Socket.io** - Библиотека для real-time двусторонней коммуникации
  - WebSocket соединения для мгновенных обновлений
  - Fallback на long-polling для совместимости
  - Комнаты и пространства имен для изоляции игр
- **PostgreSQL** - Реляционная база данных
  - Надежность и ACID-транзакции
  - JSONB для гибкого хранения (display_status, event_data)
  - Полнотекстовый поиск
  - Расширения (uuid-ossp)

### Frontend

#### Админ-панель
- **React** - UI библиотека
- **Next.js** - React фреймворк
  - Server-Side Rendering (SSR) для SEO и производительности
  - Static Site Generation (SSG) где возможно
  - API Routes для проксирования запросов
  - Оптимизация изображений и автоматический code splitting
- **TypeScript** - Типизированный JavaScript
- **Socket.io Client** - Для real-time обновлений в админке

#### ТВ-клиент
- **React** - UI библиотека
- **Next.js** - React фреймворк
  - SSR для быстрой загрузки
  - Оптимизация для больших экранов
- **Socket.io Client** - Для получения real-time обновлений
- **Media API (HTML5 Video/Audio)** - Для воспроизведения видео/аудио контента

### Дополнительные технологии

#### Real-time коммуникация
- **Socket.io** - Основной механизм real-time обновлений
  - События: `game:update`, `round:start`, `round:update`, `score:update`, `display:change`
  - Комнаты по game_id для изоляции
  - Автоматическое переподключение

#### Медиа-контент
- **Cloud Storage** (AWS S3 / Google Cloud Storage / Cloudinary) - Хранение видео/изображений
- **CDN** - Быстрая доставка медиа-контента

#### DevOps
- **Docker** - Контейнеризация приложений
- **Docker Compose** - Оркестрация для локальной разработки
- **CI/CD** - Автоматизация деплоя

## 📊 Архитектура базы данных

### Основные таблицы

#### `experts` (Знатоки)
- Хранит информацию об экспертах
- Статусы: `active`, `inactive`, `playing`
- Поддержка аватаров и биографии

#### `viewers` (Телезрители)
- Информация о телезрителях, отправляющих вопросы
- Город, страна, контакты

#### `questions` (Вопросы)
- Вопросы от телезрителей
- Типы: `text`, `video`, `image`, `audio`
- Медиа-файлы, ключевые слова, категории
- Система одобрения вопросов

#### `games` (Игры)
- Игровые сессии
- Счет экспертов и телезрителей
- Статусы: `pending`, `active`, `paused`, `finished`, `cancelled`
- Номера сезонов и эпизодов

#### `game_experts` (Связь игр и экспертов)
- Many-to-Many связь между играми и экспертами
- Позиции за столом, капитан команды

#### `rounds` (Раунды)
- Отдельные раунды в игре
- **`display_status` (JSONB)** - Ключевое поле для ТВ-клиента:
  ```json
  {
    "content": "question",
    "media": "video_file.mp4",
    "show_question": true,
    "show_timer": true,
    "timer_seconds": 45,
    "show_experts": true,
    "show_score": true,
    "sound_playing": "question_intro.mp3"
  }
  ```
- Статусы раунда: `pending`, `question_shown`, `thinking`, `answered`, `result_shown`, `finished`

#### `round_votes` (Голоса экспертов)
- Голоса/ответы экспертов в раунде (опционально)

#### `game_events` (События игры)
- История событий для синхронизации и отладки
- JSONB поле `event_data` для гибкого хранения данных событий

### Особенности схемы

1. **UUID** - Все ID используют UUID для безопасности и распределенности
2. **JSONB** - Гибкое хранение структурированных данных (display_status, event_data)
3. **Индексы** - Оптимизированы для частых запросов
4. **Триггеры** - Автоматическое обновление `updated_at`
5. **Ограничения** - CHECK constraints для валидации данных
6. **Внешние ключи** - Референсная целостность с каскадным удалением где уместно

## 🚀 Real-time обновления

### Сценарии использования

1. **Обновление счета**
   - Событие: `score:update`
   - Данные: `{ gameId, expertsScore, viewersScore }`
   - Обновляется на ТВ-клиенте и админке мгновенно

2. **Смена вопроса**
   - Событие: `round:start`
   - Обновление `rounds.display_status`
   - ТВ-клиент получает новое состояние и воспроизводит медиа

3. **Таймер**
   - Событие: `timer:update`
   - Обновление `display_status.timer_seconds`
   - Синхронизация таймера на всех клиентах

4. **Воспроизведение звуков**
   - Событие: `sound:play`
   - Данные: `{ soundFile: "intro.mp3", volume: 0.8 }`
   - ТВ-клиент воспроизводит звук

5. **Изменение отображаемого контента**
   - Событие: `display:change`
   - Обновление `display_status.content` (question, answer, result, etc.)
   - ТВ-клиент переключает отображаемый экран

### Socket.io комнаты

- `game:{gameId}` - Комната для конкретной игры
- `admin` - Комната для админ-панели
- `tv:game:{gameId}` - Комната для ТВ-клиента конкретной игры

## 📁 Структура проекта

```
WhatWhereWhen2/
├── backend/                 # NestJS приложение
│   ├── src/
│   │   ├── modules/
│   │   │   ├── games/
│   │   │   ├── rounds/
│   │   │   ├── questions/
│   │   │   ├── experts/
│   │   │   └── viewers/
│   │   ├── common/
│   │   ├── config/
│   │   └── main.ts
│   ├── test/
│   └── package.json
├── frontend/
│   ├── admin/              # Админ-панель (Next.js)
│   │   ├── pages/
│   │   ├── components/
│   │   └── package.json
│   └── tv-client/          # ТВ-клиент (Next.js)
│       ├── pages/
│       ├── components/
│       └── package.json
├── database/
│   ├── schema.sql          # SQL схема БД
│   ├── migrations/         # Миграции (если используется)
│   └── seeds/              # Тестовые данные
└── docker-compose.yml      # Docker конфигурация
```

## 🔧 Установка и запуск

### Требования
- Node.js 18+
- PostgreSQL 14+
- npm или yarn

### Быстрый старт

**Подробная инструкция:** См. [DEPLOYMENT.md](./DEPLOYMENT.md)

1. **Создание базы данных:**
   ```bash
   createdb whatwherewhen2
   psql -d whatwherewhen2 -f database/schema.sql
   ```

2. **Backend:**
   ```bash
   cd backend
   npm install
   DB_USERNAME=$(whoami) DB_PASSWORD="" npm run start:dev
   ```
   Backend: http://localhost:3002

3. **Админка:**
   ```bash
   cd frontend/admin
   npm install
   npm run dev
   ```
   Админка: http://localhost:3001

4. **ТВ-Клиент:**
   ```bash
   cd frontend/tv-client
   npm install
   npm run dev
   ```
   ТВ-Клиент: http://localhost:3003

### Звуковые файлы

Разместите звуковые файлы в `backend/uploads/`:
- `gong-1.mp3`, `intro.mp3`, `pause1.mp3`, `volchok.mp3`, `winners_music.mp3`

## 📝 API Endpoints (примерная структура)

### Games
- `GET /api/games` - Список игр
- `GET /api/games/:id` - Детали игры
- `POST /api/games` - Создание игры
- `PATCH /api/games/:id` - Обновление игры
- `POST /api/games/:id/start` - Начать игру

### Rounds
- `GET /api/games/:gameId/rounds` - Раунды игры
- `POST /api/games/:gameId/rounds` - Создать раунд
- `PATCH /api/rounds/:id` - Обновить раунд
- `POST /api/rounds/:id/display` - Обновить display_status

### Questions
- `GET /api/questions` - Список вопросов
- `POST /api/questions` - Создать вопрос
- `PATCH /api/questions/:id/approve` - Одобрить вопрос

### Experts
- `GET /api/experts` - Список экспертов
- `POST /api/experts` - Создать эксперта

## 🔌 Socket.io Events

### Client → Server
- `game:join` - Присоединиться к игре
- `round:start` - Начать раунд
- `round:answer` - Отправить ответ
- `display:update` - Обновить display_status
- `score:update` - Обновить счет

### Server → Client
- `game:update` - Обновление игры
- `round:start` - Начало раунда
- `round:update` - Обновление раунда
- `score:update` - Обновление счета
- `display:change` - Изменение отображаемого контента
- `timer:update` - Обновление таймера
- `sound:play` - Команда воспроизвести звук

## 🎯 Преимущества выбранного стека

1. **Быстрое обновление контента** - Socket.io обеспечивает мгновенные обновления
2. **Масштабируемость** - NestJS и PostgreSQL поддерживают рост приложения
3. **Типобезопасность** - TypeScript на всех уровнях
4. **Гибкость** - JSONB позволяет легко расширять структуру данных
5. **Производительность** - Индексы и оптимизированные запросы
6. **Developer Experience** - Современные инструменты и фреймворки

## 📄 Лицензия

MIT
